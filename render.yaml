services:
  - type: web
    name: pokemonitor-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn main:app --host 0.0.0.0 --port 10000"
    envVars:
      - key: FIREBASE_CREDENTIALS
        value: <INCOLLA QUI IL JSON se non usi env vars manuali>
